import { Navbar } from "@/components";

export default function PortalTemplate({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      <Navbar />
      {children}
    </div>
  );
}
